<h2>新規登録ページ</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :メールアドレスを入力してください %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :パスワードを設定してください %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> 文字以上の半角英数字)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :パスワードを再入力してください %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :ニックネームを入力ください %><br />
    <%= f.text_field :nickname, autocomplete: "new-nickname" %>
  </div>
  
  <div class="field">
    <%= f.label :プロフィールを入力してください %><br />
    <%= f.text_area :profile, autocomplete: "new-profile" %>
  </div>
  
  <div class="field">
    <%= f.label :あなたの夢を教えてください %><br />
    <%= f.text_field :dream, autocomplete: "new-dream" %>
  </div>
  
  <label class="form-text">夢を叶えるために、いつまでに何をやるか、３つ以上書いてみましょう</label>


  <div class="field">
    <%= f.label :いつまでに%><br />
    <%= f.collection_select(:first_limit_id, Limit.all, :id, :name, {}, {class:"select-box"}) %>
  </div>

  <div class="field">
    <%= f.label :何をやるか書きましょう %><br />
    <%= f.text_field :first_process, autocomplete: "new-process" %>
  </div>
 
  <div class="field">
    <%= f.label :いつまでに%><br />
    <%= f.collection_select(:second_limit_id, Limit.all, :id, :name, {}, {class:"select-box"}) %>
  </div>
  
  <div class="field">
    <%= f.label :何をやるか書きましょう %><br />
    <%= f.text_field :second_process, autocomplete: "new-process" %>
  </div>

    <div class="field">
    <%= f.label :いつまでに%><br />
    <%= f.collection_select(:third_limit_id, Limit.all, :id, :name, {}, {class:"select-box"}) %>
  </div>
  
  <div class="field">
    <%= f.label :何をやるか書きましょう %><br />
    <%= f.text_field :third_process, autocomplete: "new-process" %>
  </div>

    <div class="field">
    <%= f.label :いつまでに%><br />
    <%= f.collection_select(:fourth_limit_id, Limit.all, :id, :name, {}, {class:"select-box"}) %>
  </div>
  
  <div class="field">
    <%= f.label :何をやるか書きましょう %><br />
    <%= f.text_field :fourth_process, autocomplete: "new-process" %>
  </div>

    <div class="field">
    <%= f.label :いつまでに%><br />
    <%= f.collection_select(:fifth_limit_id, Limit.all, :id, :name, {}, {class:"select-box"}) %>
  </div>
  
  <div class="field">
    <%= f.label :何をやるか書きましょう %><br />
    <%= f.text_field :fifth_process, autocomplete: "new-process" %>
  </div>

  <div class="actions">
    <%= f.submit "新規登録" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
